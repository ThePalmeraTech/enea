<% if user_signed_in? && (current_user.role == "admin" || current_user.role == "publisher") %>
  <header class="d-none d-md-block">
          <div class="container">
            <nav id="navigation">

                <div class="d-flex justify-content-end">
                   <div class="mt-3"><h6>Hola <b><%= current_user.username.capitalize %></b> |  </div>

                    <div>
                      <%= link_to "Agregar publicacion", new_article_path, class: 'btn btn-primary btn-sm mt-2 m-2' %>
                      <%= link_to 'Sign out  ', destroy_user_session_path, class: 'btn btn-primary btn-sm mt-2 m-2', data: { turbo_method: :delete } %>
                    </div>


                </div>


            </nav>
          </div>
        </header>

<% end %>
